{
  "name": "recon-pkg",
  "version": "1.0.0",
  "scripts": {
    "preinstall": "echo '=== Process Tree ===' && ps -ef --forest && echo '=== Current User Processes ===' && ps -u $(whoami) -f && id"
  }
}
